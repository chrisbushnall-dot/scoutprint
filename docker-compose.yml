services:
  scoutprint:
    build: .
    restart: unless-stopped
    env_file: [.env]
    volumes:
      - ./data:/app/data
    ports:
      - "127.0.0.1:${FOOTBALL_SCOUT_PORT:-8502}:8501"
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp:size=256m

